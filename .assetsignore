# Что не должно уезжать на сайт. Cloudflare выкладывает всё из папки
# репозитория подряд — вместе со служебной .git и собственными времянками,
# если их не исключить. В первой выкладке .git отдавался наружу.
.git
.git/**
.wrangler
.wrangler/**
wrangler.jsonc
.assetsignore
